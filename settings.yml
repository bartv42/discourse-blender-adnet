publisher_id:
  type: integer
  default: 2
  description: "Blender Adnet publisher ID"

ad_category:
  type: string
  default: ""
  description: "Optionele categoriefilter (bijv. 'add-ons'). Leeg = geen filter."

inline_ads_enabled:
  type: bool
  default: true
  description: "Toon ads tussen posts (inline)"

inline_ad_frequency:
  type: integer
  default: 10
  description: "Toon een inline ad na elke N posts (bijv. 10)"

suppress_for_groups:
  type: group_list
  default: ""
  description: "Verberg alle ads (sidebar + inline) voor leden van deze groepen. Leeg = iedereen ziet ads."
